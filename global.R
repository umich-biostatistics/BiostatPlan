
# global objects shared between UI and server

# List of all available classes
classes = list(
  "Bios 601", "Bios 602", "Health 222"
)

selections = list(
  "FY1" = data.frame(),
  "SY1" = data.frame(),
  "FY2" = data.frame(),
  "SY2" = data.frame()
)

print_selection = function(semester) {
  
}
